---
name: alias-skill
version: 1.0.0
description: Uses alternative section heading aliases.
---

## Prerequisites

- `git` - version control tool
- config.yaml - configuration file path

## Create artifact

Uses `git` to produce `output.zip`.

## Produces

- output.zip - the generated archive
