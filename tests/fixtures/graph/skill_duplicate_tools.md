---
name: skill-duplicate-tools
description: "Runs shell commands for the build pipeline using the same tool listed twice."
allowed-tools: [Bash, Bash, Read]
---
# Duplicate tools

## Run build

Runs the build with `Bash`.
